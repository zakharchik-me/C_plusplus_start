#include <iostream>
#include "Point.h"

int main() {
    std::cout << "Hello, World!" << std::endl;
    std::cout << "none_of = " << none_of(onlyOne.cbegin(), onlyOne.cend(), eqOne<int>) << std::endl;
    std::cout << "is_sorted = " << is_sorted(arrPointSort.cbegin(), arrPointSort.cend(), isMore<Point>) << std::endl;
    return 0;
}
